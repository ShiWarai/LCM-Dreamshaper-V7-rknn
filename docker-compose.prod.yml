# Production: pre-built image from GHCR (tag :master).
# docker pull ghcr.io/shiwarai/lcm-dreamshaper-v7-rknn:master
# docker compose -f docker-compose.yml -f docker-compose.prod.yml up -d

services:
  dreamshaper:
    image: ghcr.io/shiwarai/lcm-dreamshaper-v7-rknn:master
    build: !override null
